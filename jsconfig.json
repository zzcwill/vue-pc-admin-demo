{
  "compilerOptions": {
    "baseUrl": ".", // 设置基础路径
    "paths": {
      "@/*": ["./src/*"] // 路径别名
    }
  },
  "include": ["src/**/*"], // 包含的文件或目录
  "exclude": ["node_modules", "dist", "build"] // 排除的文件或目录
}
